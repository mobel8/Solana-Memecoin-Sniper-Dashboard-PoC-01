[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
buildContext = "backend"

[deploy]
healthcheckPath = "/api/opportunities"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
